import { describe, test } from "node:test";
import { strict as assert } from "node:assert";

describe("iterator pattern", () => {
  describe("iterator", () => {
    test("should be able to iterate over a collection", () => {});
  });
});
